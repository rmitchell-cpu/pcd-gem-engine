---
description: List all pipeline jobs with fund, state, and creation time
---

List all pipeline jobs:

```bash
python run.py --list-jobs
```

Show the output to the user as a table. If they mention a specific fund or job, offer `/pcd-gem-engine:job-status` for details.
